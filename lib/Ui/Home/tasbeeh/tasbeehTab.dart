import 'package:flutter/material.dart';

class TasbeehTab extends StatelessWidget {
  const TasbeehTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
